---
title: 🧪 Insights
contributors:
  - mhevery
---

# 🧪 Insights

**Stage:** `prototyping`

Insights позволяет вашему приложению собирать информацию об использовании от реальных пользователей для оптимизации создания бандлов вашего приложения. Наблюдая за поведением реальных пользователей, система сборки Qwik может лучше справляться с размещением символов, которые используются вместе, в одном блоке, и тем самым минимизировать эффект водопада, который может возникнуть, если необходимо загрузить слишком много маленьких файлов.

## Архитектура

Оптимизация состоит из следующих частей:

- Компонент `<Insights>`, который собирает реальные данные об использовании от пользователей;
- Размещённое приложение с базой данных (http://qwik-insights.builder.io);
- Плагин vite time, который предоставляет информацию о бандле.

## Компонент `<Insights>`

Компонент `<Insights>` должен быть добавлен в ваш файл `root.tsx`.

```typescript
// ...
import { Insights } from '@builder.io/qwik-labs';

export default component$(() => {
  // ...
  return (
    <QwikCityProvider>
      <head>
        // ...
        <Insights
          publicApiKey={import.meta.env.PUBLIC_QWIK_INSIGHTS_KEY}
        />
      </head>
      <body lang="en">
        // ...
      </body>
    </QwikCityProvider>
  );
});
```

Вы можете получить `PUBLIC_QWIK_INSIGHTS_KEY`, посетив сайт https://qwik-insights.builder.io/ и создав приложение.

Компонент `<Insights>` собирает следующие данные:

- Информация времени у символов;
- Часть URL-адреса, содержащая `pathname`;
- Случайный идентификатор сессии, который определяет, какие символы загружены из одной и той же сессии браузера.

ПРИМЕЧАНИЕ: Компонент `<Insights>` не собирает никакой идентифицирующей пользователя информации.

Собранная информация отправляется на сайт https://qwik-insights.builder.io/ для хранения.

## Размещённое приложение

Сайт https://qwik-insights.builder.io/ является частью программы Insights, и хранит статистическую информацию об использовании символов приложения. Реализация сервиса имеет открытый исходный код, и у вас есть выбор - использовать наш или разместить свой собственный (обратите внимание, что в будущем эта услуга может стать платной).

## Интеграция Vite

Когда приложение работает некоторое время и собирает достаточно информации об использовании символов, статистику можно использовать для улучшения сборки бандлов будущей версии приложения. Это делается путём соединения сборки Vite с Insights следующим образом:

file: `vite.config.js`
```typescript
//..
import { defineConfig, loadEnv } from 'vite';
import { Insights } from '@builder.io/qwik-labs';


export default defineConfig(async () => {
  return {
    plugins: [
      qwikVite({
        entryStrategy: await insightsEntryStrategy({
          publicApiKey: loadEnv('', '.').PUBLIC_QWIK_INSIGHT_KEY,
        }),
      }),
      //...
    ],
    // ...
  };
});
```
