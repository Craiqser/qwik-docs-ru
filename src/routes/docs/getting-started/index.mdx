---
title: Начало работы
contributors:
  - manucorporat
  - jesperp
  - adamdbradley
---

# Начало работы с Qwik

Qwik - это новый вид фреймворка, который является [возобновляемым](./../concepts/resumable/index.mdx) (без JS и гидратации), современным и [знакомым для разработчиков на React](./../cheat/qwik-react/index.mdx).

- [Stackblitz Qwik + Vite](https://qwik.new)
- [Песочница с примерами](https://qwik.builder.io/examples/reactivity/counter/)

## Подготовка

- [Node.js v14](https://nodejs.org/en/download/) или выше
- Ваша любимая IDE (рекомендуется [VSCode](https://code.visualstudio.com/))
- Начните [думать как Qwik](./../think-qwik/index.mdx)

## Создание приложения с помощью CLI

Первый шаг - создание приложения. Qwik поставляется с CLI (CLI - Command Line Interface - интерфейс командной строки), который позволяет вам создать базовый рабочий скелет приложения. Мы будем использовать CLI для создания стартового приложения, чтобы вы могли быстрее познакомиться с ним.

### Запуск Qwik CLI в терминале

```shell
npm create qwik@latest
```

CLI проведёт вас через интерактивное меню, чтобы задать название проекта и выбрать один из стартовых шаблонов:

<script id="asciicast-514542" src="https://asciinema.org/a/514542.js" async></script>

После создания нового приложения вы увидите в терминале вывод, подобный следующему:

```shell
💫 Let's create a Qwik app 💫

✔ Project name … qwik-app
✔ Select a starter › Basic

🦄  Success!  Project created in portfolio directory

🐰 Next steps:
   cd qwik-app
   npm install
   npm start

💬 Questions? Start the conversation at:
   https://qwik.builder.io/chat
   https://twitter.com/QwikDev

📺 Presentations, Podcasts and Videos:
   https://qwik.builder.io/media/
```

На этом этапе у вас будет каталог `qwik-app`, содержащий стартовое приложение.

## Запуск в режиме разработки

После того, как файлы приложения будут созданы, требуется выполнить следующие шаги:

1. Перейдите в каталог, созданный командой `npm create qwik@latest`

```shell
cd qwik-app
```

2. Установите модули NPM

```shell
npm install
```

3. Запустите сервер приложения в режиме разработки

```shell
npm start
```

4. Вы должны увидеть баннер запущенного сервера

```shell

  VITE v3.1.1  ready in 140 ms

  ➜  Local:   http://localhost:5174/
  ➜  Network: use --host to expose

```

5. Перейдите в браузере по адресу http://localhost:5174/, чтобы ознакомиться с приложением.

## Команды

Вот несколько полезных команд для начала работы:

- `npm start`: алиас для команды `npm run dev`, базовая команда, запуск с поддержкой SSR;
- `npm run preview`: Production-сборка и запуск в режиме предварительного просмотра;
- `npm run build`: Production-сборка приложения;
- `npm run qwik add`: Выбор и добавление в проект пакета интеграции (на текущий момент это __Server Adaptors (SSR)__, __Static Generator (SSG)__, [Partytown](https://www.npmjs.com/package/@builder.io/partytown), [Qwik React](https://www.npmjs.com/package/@builder.io/qwik-react), [Tailwind](https://www.npmjs.com/package/tailwindcss)).

Полный список команд можно найти в файле `package.json`.
