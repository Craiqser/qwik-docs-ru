---
title: Qwik City адаптеры и промежуточное ПО
contributors:
  - adamdbradley
---

# Адаптеры и промежуточное ПО

Промежуточное ПО Qwik City - это код, который соединяет серверный фреймворк рендеринга (такой как Cloudflare, Netlify, Express и т.д.) с мета-фреймворком Qwik City.

С Qwik City в комплекте поставляются:

- [cloudflare-pages](../cloudflare-pages/index.mdx)
- [netlify-edge](../netlify-edge/index.mdx)
- [node](../node/index.mdx)

## Сборка

Используйте Qwik City vite cli с аргументом `--ssr` для указания точки входа для SSR:

```bash
vite build --ssr src/entry.MIDDLEWARE.ts
```

Каталогом вывода по умолчанию является `./server`, он настраивается путём установки опции плагина `qwikVite`.

```typescript
// Файл vite.config.ts
import { qwikVite } from '@builder.io/qwik/optimizer';

export default defineConfig(() => {
  return {
    plugins: [
      // ...другие плагины
      qwikVite({
        ssr: {
          outDir: 'CUSTOM_DIR',
        },
      }),
    ],
  };
});
```

## Дополнительно

Утилита `requestHandler()` - это то, что использует каждый из вышеперечисленных промежуточных модулей для преобразования своих запросов/ответов в формат Qwik City. Функция может быть использована для предоставления промежуточного ПО под конкретный серверный фреймворк.

Если вам не хватает определённого промежуточного ПО, и вы хотели бы его добавить сами, посмотрите, как утилита `requestHandler()` используется для обработки запросов для каждого из приведенных выше примеров. А ещё лучше - внесите свой вклад, мы будем рады! [PR приветствуются](https://github.com/BuilderIO/qwik/tree/main/packages/qwik-city/middleware)!
