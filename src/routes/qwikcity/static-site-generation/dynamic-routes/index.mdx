---
title: Динамические маршруты при генерации статических сайтов
contributors:
  - adamdbradley
---

# Динамические маршруты SSG

```typescript
import type { StaticGenerateHandler } from '@builder.io/qwik-city';

export default component$(() => {
  const { params } = useLocation();

  return <div>Пример: {params.id}</div>;
});

export const onStaticGenerate: StaticGenerateHandler = () => {
  // Реализация загрузки данных по id (закодируем ответ для упрощения).
  const ids = [...]; // Перечень id для генерации.

  return {
    params: ids.map((id) => {
      return { id };
    }),
  };
};
```
