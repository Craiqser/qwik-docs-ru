---
title: Qwik City и PostCSS
keywords: 'styles, styling'
contributors:
  - manucorporat
  - manuelsanchezweb
  - manuelsanchez2
  - the-r3aper7
---

# PostCSS

[PostCSS](https://postcss.org/) позволяет разработчикам писать современный CSS, сохраняя при этом обратную совместимость и повышая производительность. Он анализирует CSS-код и пропускает его через серию плагинов, которые могут изменять, оптимизировать и расширять CSS, прежде чем вывести конечный результат.

## Использование

Вы можете добавить PostCSS в свой проект с помощью следующего стартового шаблона Qwik:

```shell
npm run qwik add postcss
```

Это создаст новый файл `postcss.config.js` в корне проекта со следующими зависимостями и конфигурацией.

- [Autoprefixer](https://github.com/postcss/autoprefixer)
- [PostCSS Preset Env](https://preset-env.cssdb.org/)

```javascript
module.exports = {
  plugins: {
    autoprefixer: {},
    "postcss-preset-env": {
      stage: 3,
      features: {
        "nesting-rules": true,
      },
    },
  },
}
```

Если вы хотите добавить новый плагин, такой как [CssNano](https://cssnano.co/), выполните следующую команду и обновите `postcss.config.js`.

```shell
npm install cssnano --save-dev
```

```javascript
module.exports = {
  plugins: {
    /* предыдущая конфигурация плагина */
    "cssnano": {
      preset: "default"
    }
  },
}
```

Для получения дополнительной документации ознакомтесь с [PostCSS Docs](https://postcss.org/docs/)
