---
title: Qwik City - Промежуточное ПО Google Cloud Run
contributors:
  - DustinJSilk
---

# Адаптер Google Cloud Run

Адаптер Qwik City Cloud Run позволяет запускать приложения Qwik City [Google Cloud Run](https://cloud.google.com/run).

## Установка

Чтобы интегрировать адаптер `Cloud Run`, используйте команду `add`:

```shell
npm run qwik add cloud-run
```

## Сборка рабочей среды

Чтобы собрать приложение для рабочей среды, используйте команду `build`, эта команда автоматически запустит `npm run build.server` и `npm run build.client`:

```shell
npm run build
```

## Развёртывание в рабочей среде

После запуска `npm run build` у вас будет 2 папки сборки:

- Будет создана папка `dist`, содержащая все статические файлы;
- Будет создана папка `server`, включающая все файлы сервера Node.

Чтобы развернуть сервер, убедитесь, что вы создали учётную запись и прошли аутентификацию в gcloud CLI.

Вам также нужно будет обновить название приложения Cloud Run для скрипта `deploy` в файле package.json.

Затем вы можете выполнить эту команду для развёртывания вашего приложения:

`npm run deploy`
